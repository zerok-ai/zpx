apiVersion: builtin
kind: PrefixTransformer
metadata:
  name: image-prefix

prefix: ${VIZIER_ARTIFACT_REPO}/

fieldSpecs:
- path: spec/template/spec/containers/image
- path: spec/image  # for kind Prometheus and Alertmanager